spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/services
  #              другие конфиги будет лежать в папке "/services"
  profiles:
    active: native
  security:
    user:
      password: admin

server:
  port: 8001